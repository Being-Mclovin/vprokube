apiVersion: v1
kind: Service
metadata:
  name: {{.Values.memecachedservice.name}}
spec:
  selector:
    app: {{.Values.memecachedservice.selector.app}}
  ports:
  - port: {{.Values.memecachedservice.port}}
    targetPort: vpromc-port
    protocol: TCP
  type: ClusterIP